name: Test and Build
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: usebuntu-latest

    steps:
        - name: 'hello world'
          run: echo "Hello, world!